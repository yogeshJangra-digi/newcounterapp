{
  "name": "webhook-service",
  "version": "1.0.0",
  "description": "A webhook service for auto-updating Git repositories",
  "main": "webhook-service.js",
  "scripts": {
    "start": "node webhook-service.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "express": "^4.18.2",
    "webhook-service": "file:"
  }
}
